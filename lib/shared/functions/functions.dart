import 'package:flutter/material.dart';

double getScreanHeight(context) {
  return MediaQuery.of(context).size.height;
}

double getScreanWeidth(context) {
  return MediaQuery.of(context).size.width;
}
